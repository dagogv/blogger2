<h1>Articles Tagged with <%= @tag.name %></h1>

<%= @tag.name %>

<hr><br><br>
<%= link_to 'Back to Articles', articles_path %><br>
<%= link_to 'Delete', tag_path(@tag), method: :delete, data: {confirm: 'Delete tag?'} %><br>
<%= link_to 'Edit', edit_tag_path(@tag) %>